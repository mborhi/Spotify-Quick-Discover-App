import { loadGenres } from "../utils/fetch-genres";

describe('Fetch genres from database or make Spotify API call', () => {
    it('correctly retreives all genres from the database', () => {

    })
})