import { checkForRefresh } from "../utils/refreshToken";

describe('Test access token expiration check', () => {
    it('correctly validates refresh time', () => {

    });

});

// describe('Test refreshed token retreival', () => {

// });